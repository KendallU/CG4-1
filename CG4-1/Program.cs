using System;

namespace CG4_1
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Pick a number,");
            Console.Write("Type number here:");
            string userValue;
            userValue = Console.ReadLine();

            int xi = int.Parse(userValue);




            for(int xi = 0; xi < 20; xi++) ;
            {
                if (userValue * xi == 0)
            }

            Console.WriteLine(xi);
        }
                       
        }
    }
}
